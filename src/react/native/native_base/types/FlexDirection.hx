package react.native.native_base.types;

enum abstract FlexDirection(String) {
	var row;
	var row_reverse = "row-reverse";
	var column;
	var column_reverse = "column-reverse";
	var initial = 'initial';
	var inherit = 'inherit';
}
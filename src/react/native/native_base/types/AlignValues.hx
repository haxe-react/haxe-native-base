package react.native.native_base.types;

enum abstract AlignValues(String) {
	var auto;
	var stretch;
	var center;
	var flex_start = "flex-start";
	var flex_end = "flex-end";
	var baseline;
	var initial;
	var inherit;
}
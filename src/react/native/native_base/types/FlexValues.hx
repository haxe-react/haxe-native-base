package react.native.native_base.types;

enum abstract FlexValues(String) {
	var flex_grow = "flex-grow";
	var flex_shrink = "flex-shrink";
	var stretch;
	var center;
	var flex_start = "flex-start";
	var flex_end = "flex-end";
	var baseline;
	var initial;
	var inherit;
}